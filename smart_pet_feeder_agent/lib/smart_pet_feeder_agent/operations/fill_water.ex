defmodule FillWater do
    
    def request(_params) do
      #TODO: Start pump and fill water until full.
      response = "Got something as a resposne from the operation."
      ResponseHandler.build_response(response)
    end

end